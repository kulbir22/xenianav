import React from 'react';
import {
    View
} from 'react-native';

    class SwipeTest extends React.Component {
        constructor(props) {
            super(props)
            this.state = {
            
            }
        }

    render() {
        console.log("@@@!!!$$$ SWIPE TEST PAGE");
        return (
            <View style={{flex: 1, backgroundColor: 'red'}}>
            </View>   
        )}
    }
export default SwipeTest;